From ddf5d29bc0a4af0e7ebd90d222cbf6ed55d57097 Mon Sep 17 00:00:00 2001
From: jchaloup <jchaloup@redhat.com>
Date: Tue, 3 Jun 2014 13:17:57 +0200
Subject: [PATCH] nscd.conf netgroup caching information missing

---
 man-pages/man5/nscd.conf.5 | 39 ++++++++++++++++++++++++++++++++++++---
 1 file changed, 36 insertions(+), 3 deletions(-)

diff --git a/man-pages/man5/nscd.conf.5 b/man-pages/man5/nscd.conf.5
index c883238..7a0e5a0 100644
--- a/man-pages/man5/nscd.conf.5
+++ b/man-pages/man5/nscd.conf.5
@@ -34,7 +34,7 @@ A \(aq#\(aq (number sign) indicates the beginning of a
 comment; following characters, up to the end of the line,
 are not interpreted by nscd.
 
-Valid services are \fIpasswd\fP, \fIgroup\fP, or \fIhosts\fP.
+Valid services are \fIpasswd\fP, \fIgroup\fP, \fIhosts\fP, \fIservices\fP or \fInetgroup\fP.
 
 .B logfile
 .I debug-file-name
@@ -46,6 +46,7 @@ Specifies name of the file to which debug info should be written.
 .I value
 .RS
 Sets the desired debug level.
+The default is 0.
 .RE
 
 .B threads
@@ -71,6 +72,7 @@ option is ignored.
 Enables or disables the specified
 .I service
 cache.
+The default is no.
 .RE
 
 .B positive-time-to-live
@@ -108,6 +110,7 @@ to reduce cache coherency problems.
 This is the internal hash table size,
 .I value
 should remain a prime number for optimum efficiency.
+The default is 211.
 .RE
 
 .B check-files
@@ -120,14 +123,17 @@ for changes.
 The files are
 .IR /etc/passwd ,
 .IR /etc/group ,
+.IR /etc/hosts ,
+.IR /etc/services
 and
-.IR /etc/hosts .
+.IR /etc/netgroup .
+The default is yes.
 .RE
 
 .B max-threads
 .I threads
 .RS
-Specifies the maximum number of threads to be started.
+Specifies the maximum number of threads. The default is 32.
 .RE
 
 .B stat-user
@@ -149,6 +155,7 @@ The default is 5.
 .I <yes|no>
 .RS
 Enabling paranoia mode causes nscd to restart itself periodically.
+The default is no.
 .RE
 
 .B restart-interval
@@ -160,6 +167,7 @@ seconds
 if periodic restart is enabled by enabling
 .B paranoia
 mode.
+The default is 3600.
 .RE
 
 .B persistent
@@ -171,6 +179,7 @@ Keep the content of the cache for
 over server restarts; useful when
 .B paranoia
 mode is set.
+The default is no.
 .RE
 
 .B shared
@@ -182,6 +191,30 @@ The memory mapping of the nscd databases for
 is shared with the clients so
 that they can directly search in them instead of having to ask the
 daemon over the socket each time a lookup is performed.
+The default is no.
+.RE
+
+.B max-db-size
+.I service
+.I number-of-bytes
+.RS
+The maximum allowable size of the database files in bytes for the
+.IR service .
+The default is 33554432.
+.RE
+
+.B auto-propagate
+.I service
+.I <yes|no>
+.RS
+When set to
+.IR no " for " passwd " or " group " service, then the " .byname
+requests are not added to
+.IR passwd.byuid " or " group.bygid
+cache.
+This may help for tables containing multiple records for the same id.
+The default is yes. This option is valid only for services
+.IR passwd " and " group ".
 .RE
 .SH "SEE ALSO"
 .BR nscd (8)
-- 
1.9.3

